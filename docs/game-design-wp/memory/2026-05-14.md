# Session Memory — 2026-05-14

## Context
Workspace: `/home/khoivu/Project/PhamNhanOnline/docs/game-design-wp/`
Key files: `shared-rules.md`, `features/*.md`, `requirements/*.md`, `notes/deferred-features.md`

---

## Session Summary

### Major milestones this session
- Cleared **all 25 feature docs** to READY status (excluding cultivation-and-breakthrough special case)
- `world-boss-system` cleared via Dev consult: grounded boss behavior per runtime code
- `cultivation-and-breakthrough` cleared: confirmed not full-promote candidate; downstream is requirement for penalty refactor only
- Group consult infrastructure confirmed working (was failing due to network/outbound block; DevOps fixed it)
- Full flow test: promoted `herb-farming-system` to `requirements/herb-farming-system.md`

### Feature docs status
- **24 feature docs: READY to promote to requirements**
- **world-boss-system: READY** (cleared this session)
- **cultivation-and-breakthrough: special grounding only** — downstream = `requirements/cultivation-penalty-refactor.md`
- **herb-farming-system: PROMOTED** — `requirements/herb-farming-system.md` exists

---

## Key Decisions This Session

### World boss
- Phase trigger: cho phép HP / time / event, config per boss per map
- Target priority: pendingAggroOverride first, then nearest valid in range
- Out-of-combat restore: boss giữ HP khi về patrol (không full heal khác enemy thường)
- Skill cadence: timer-based, round-robin, target = current combat target
- Random chase: boss-specific extension — đuổi ngẫu nhiên khi không có ai tấn công/đứng gần; khi có target thì dùng proactive attack framework như enemy thường
- Participation reward: config per boss/reward, không cố định toàn hệ

### Herb farming / linh dược
- Mỗi phẩm cấp linh dược = item template riêng (confirmed by TechDesign)
- `required_herb_maturity` deprecated — recipe phải gate bằng item template identity
- Client UI/handler wiring verification: non-blocking for requirement promotion (user confirmed)

### Cultivation and breakthrough
- Không promote full requirement
- Downstream: tạo `requirements/cultivation-penalty-refactor.md` riêng
- Penalty refactor: có thể là isolated refactor trừ khi `potential_reward_locked` đang được dùng cho reconnect/retry/anti-abuse

### Requirement promotion workflow
- Feature doc: KHÔNG xóa khi promote — là canonical design truth
- Requirement doc: tạo mới trong `requirements/`, cụ thể hơn feature
- Requirement thêm: REQ-*, AC-*, state/lifecycle, rules/invariants, runtime flow, data/config, telemetry, known drift

---

## Pending — Chờ User Input

### 4 rule herb-farming-system đã chốt
1. **Inventory full khi harvest/extract**: **reject toàn bộ action** — không inbox overflow cho harvest/extract.
2. **Herb/item hết hạn**: **xóa luôn** — không có spoiled item.
3. **Mầm non nguồn**: **nhiều nguồn** — extract return, drop quái, mua NPC — cùng item template, cùng replant flow.
4. **Plot count**: **fixed config per blueprint grade** — data-driven, không cần runtime logic.

### Template update cần apply
User yêu cầu bổ sung vào requirement template và tất cả requirement docs sau này:
1. **Target Design Summary** — behavior muốn đạt
2. **Current Runtime / Evidence Snapshot** — đã confirm / chưa confirm / cần verify trong code
3. **Known Conflicts / Drift** — design lệch code, legacy behavior, migration caveats
4. **Readiness Level** — Ready for TechDesign refinement / Dev handoff / QA verification

Và chốt rõ hơn trong requirement:
- Inventory full handling cho harvest/extract
- Item hết hạn: mất hay spoiled
- Replant nguồn
- Plot count implementation approach

`requirements/herb-farming-system.md` cần update theo template mới khi user trả lời 4 câu hỏi trên.

---

## CRITICAL WORKFLOW RULE — PROMOTE TO REQUIREMENT

Khi GameDesign promote feature doc lên requirement, bắt buộc phải làm ĐỦ 3 bước:
1. Tạo `requirements/<system>.md`
2. Tạo handoff file `docs/agent-handoffs/active/<date>-<system>-<owner>.md`
3. Thêm dòng vào `docs/agent-handoffs/QUEUE.md`

Nếu thiếu bước 2 hoặc 3, TechDesign agent sẽ không biết có việc và flow bị nghẽn.
Không được kết thúc promote mà chưa làm đủ 3 bước.

## Workflow Rules (confirmed this session)
- Khi cần hỏi Dev/TechDesign: dùng group-consult-send, chờ reply, mang về hỏi user trước khi chốt vào doc
- Không tự bịa câu trả lời từ Dev/TechDesign
- Không promote doc khi chưa có user approval
- Feature doc không bị xóa khi promote requirement
- Requirement doc phải cụ thể hơn feature doc (không chỉ đổi trạng thái)

---

## Group Consult Infrastructure
- Helper scripts: `/home/khoivu/.openclaw/bin/group-consult-send` và `group-consult-read`
- Group ID: `-5114113700`
- Token: `/home/khoivu/.openclaw/secrets/gamedesign_bot.token`
- Flow: send -> capture consultId -> read --id <id> --timeout 120
- Status: WORKING (fixed by DevOps this session)

---

## Next Steps (priority order)
1. User trả lời 4 câu hỏi về herb-farming-system rule
2. Update `requirements/herb-farming-system.md` theo template mới + 4 rule đã chốt
3. Update `templates/requirement-spec-template.md` theo template mới
4. Promote các feature docs còn lại sang requirements/
5. Tạo `requirements/cultivation-penalty-refactor.md` cho penalty refactor
